"""grounding — the shared grounding-core (Decision D / SCOPE §10-D).

The factored heart both write_enrichment and link_events import. It implements the
corruption-proof boundary as reusable mechanics: resolve a Fragment (handle / current /
cross-check id), code-slice the Evidence Span, run the tier-aware judge, gate on the
verdict, and freeze the immutable Provenance Validation attestation (hashes of the exact
bytes grounded against).

The model POINTS; code COPIES the exact tokens; the judge CHECKS; the row ATTESTS.
Nothing here ever trusts a model-typed exact token.
"""
